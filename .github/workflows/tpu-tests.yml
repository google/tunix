# Copyright 2025 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#    https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: TPU Tests

on:
  workflow_call:
    inputs:
      dev_image:
        description: 'Docker image URI for run_dev (defaults to us-central1-docker.pkg.dev/cloud-tpu-multipod-dev/tunix/mlperf:latest)'
        required: false
        type: string
        default: 'us-central1-docker.pkg.dev/cloud-tpu-multipod-dev/tunix/mlperf:latest'
    secrets:
      HF_TOKEN:
        description: 'HuggingFace token for model downloads'
      KAGGLE_USERNAME:
        description: 'Kaggle Username'
      KAGGLE_KEY:
        description: 'Kaggle API Key'

concurrency:
  # Dedup pull requests (canceling previous runs of the same workflow for same PR), and scheduled runs but nothing else
  group: ${{ github.event_name == 'pull_request' && format('{0}-pr-{1}', github.workflow, github.event.pull_request.number) || github.event_name == 'schedule' && format('{0}-schedule', github.workflow) || github.run_id }}
  cancel-in-progress: true

# Pin cache roots inside the container (`/root/.cache/...`) so `actions/cache`
# restores and persists data at the exact paths read by `huggingface_hub`,
# `kagglehub`, `tensorflow_datasets`, `jax`, and `pip` without baking any
# weights or datasets into the production `tunix/mlperf` Docker image.
env:
  # TODO(tunix-dev): Mirror large (>7B) Hugging Face models via a regional GCS
  # bucket or runner-local persistent disk to avoid cross-run CDN variance
  # without exceeding GitHub's 10 GB repository cache quota.
  HF_HOME: /root/.cache/huggingface
  KAGGLEHUB_CACHE: /root/.cache/kagglehub
  HF_HUB_ENABLE_HF_TRANSFER: "1"
  HF_XET_HIGH_PERFORMANCE: "1"
  JAX_COMPILATION_CACHE_DIR: /root/.cache/jax_compilation_cache
  PIP_CACHE_DIR: /root/.cache/pip
  TFDS_DATA_DIR: /root/.cache/tensorflow_datasets

# Jobs here are grouped by container image, not by test area. Each job holds an
# entire 8-chip v6e host for its whole duration, and the self-hosted TPU runner
# pool is shared, so the number of jobs -- not their runtime -- is what
# determines how much of that pool a single PR occupies.
#
# Adding a test: put it in the job whose image it needs. Only add a new job if it
# needs a third image, and say why in the review.
#
# Within a job, steps that mutate the Python environment (force-reinstalls, extra
# framework installs) MUST come last, so they cannot perturb the tests above them.
jobs:
  # ---------------------------------------------------------------------------
  # Job: run_core
  # Purpose: Multi-device TPU unit tests, then SGLang-JAX integration.
  # Hardware: Live TPU v6e-8 accelerator (1 slice).
  # Image:    JAX stable stack candidate.
  # Secrets:  HF_TOKEN, used only by the SGLang stage.
  # Scope:    Sharded model, generation, SFT, distillation and RL unit tests,
  #           followed by SGLang sampler, LoRA and RL cluster integration tests.
  #
  # ORDERING CONSTRAINTS (within run_core):
  #   1. Core unit test suites (generation, SFT, distillation, RL cluster, uncovered)
  #      MUST run BEFORE SGLang-JAX so they execute against the unmodified candidate
  #      JAX stable stack image. (Relative order among these 5 suites is flexible.)
  #   2. SGLang-JAX (`Run install sglang-jax && test`) MUST run LAST in this job.
  #      It installs `sglang-jax` and pins `jax[tpu]<0.11.0` and `qwix`, mutating
  #      the Python environment.
  # ---------------------------------------------------------------------------
  run_core:
    runs-on: [linux-x86-ct6e-180-8tpu]
    environment: testing
    # Note: GitHub Actions `timeout-minutes` only counts active execution time after
    # a runner picks up the job (queue wait time is excluded). Measured execution
    # is ~14.2m, so 60m provides >4x headroom while catching hung processes.
    timeout-minutes: 60
    container:
      image: us-docker.pkg.dev/tpu-prod-env-multipod/jax-stable-stack/candidate/tpu:latest # zizmor: ignore[unpinned-images]
      options: --privileged
      env:
        CLOUD_TPU_ACCELERATOR: v6e-8
        JAX_PLATFORMS: tpu
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Install zstd for fast cache compression
        run: |
          which zstd || (apt-get update -qq && apt-get install -y -qq --no-install-recommends zstd)
          rm -f /etc/pip.conf /root/.config/pip/pip.conf /opt/venv/pip.conf ~/.pip/pip.conf || true
          mkdir -p /root/.cache/pip
          python3 -m pip config --global set global.cache-dir /root/.cache/pip

      # Cache pip wheels. Without this the job re-downloads its whole dependency
      # tree while holding a v6e-8 host.
      - name: Cache pip
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: /root/.cache/pip
          key: pip-core-v5-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'scripts/install_*.sh') }}
          restore-keys: |
            pip-core-v5-${{ runner.os }}-

      - name: Cache JAX compilation
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: /root/.cache/jax_compilation_cache
          key: jax-cache-core-v4-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'scripts/install_*.sh') }}
          restore-keys: |
            jax-cache-core-v4-${{ runner.os }}-
            jax-cache-core-v3-${{ runner.os }}-

      # Restore Hugging Face weights from the shared `hf-maxtext-dev-v3` cache so
      # `sglang_jax_sampler_test.py` and `sglang_jax_lora_test.py` do not
      # re-download `Llama-3.2-1B-Instruct` and `Qwen2.5-0.5B-Instruct` on every run.
      - name: Cache Hugging Face models
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: |
            /root/.cache/huggingface
            !/root/.cache/huggingface/xet
            !/root/.cache/huggingface/hub/models--Qwen--Qwen2.5-7B*
          key: hf-core-v3-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            hf-core-v3-${{ runner.os }}-
            hf-maxtext-dev-v3-${{ runner.os }}-

      - name: Install tunix dependencies
        run: |
          unset PIP_NO_CACHE_DIR
          export PIP_NO_BUILD_ISOLATION=1
          pip install -e ".[test]"

      - name: Verify TPU availability
        run: |
          python -c "
          import jax
          print(f'JAX version: {jax.__version__}')
          print(f'JAX devices: {jax.devices()}')

          # Check if we have TPU devices specifically
          devices = jax.devices()
          has_tpu = len(devices) > 0 and all(device.platform == 'tpu' for device in devices)
          print(f'TPU available: {has_tpu}')

          if not has_tpu:
              print('ERROR: No TPU devices found! Expected TPU devices but got:', [device.platform for device in devices])
              exit(1)
          else:
              print(f'SUCCESS: Found {len(devices)} TPU device(s)')
          "

      - name: Run tunix generation tests (PASSED only)
        run: |
          # tokenizer_adapter_test requires access to gated repo
          python -m pytest tests/generate/ -v --tb=short \
            --ignore=tests/generate/vllm_sampler_test.py \
            --ignore=tests/generate/vllm_sampler_qwen_test.py \
            --ignore=tests/generate/vllm_driver_test.py \
            --ignore=tests/generate/tokenizer_adapter_test.py \
            --ignore=tests/generate/sglang_jax_sampler_test.py \
            --ignore=tests/generate/sglang_jax_lora_test.py

      - name: Run tunix SFT tests
        run: |
          python -m pytest tests/sft/ -v --tb=short

      - name: Run tunix distillation tests
        run: |
          python -m pytest tests/distillation/ -v --tb=short

      - name: Run tunix RL cluster tests
        run: |
          python -m pytest tests/rl/rl_cluster_test.py -v --tb=short

      - name: Run tunix tests not covered by the above categories
        run: |
          # This category is to catch tests added but not covered by CI yet. Whenever you add new folders under tests/, please add a new category above and skip those tests here.
          python -m pytest tests/ -v --tb=short --ignore=tests/perf/ --ignore=tests/model_alignment/ --ignore=tests/models/ --ignore=tests/cli/ --ignore=tests/utils/mesh_utils_test.py --ignore=tests/utils/topology_test.py --ignore=tests/generate/ --ignore=tests/sft/ --ignore=tests/distillation/ --ignore=tests/rl/ --ignore=tests/smoke_tests/ --ignore=tests/experimental/ || code=$?
          if [ "${code:-0}" = "5" ]; then
            echo "No tests collected (expected)."
            exit 0
          else
            exit "${code:-0}"
          fi

      # --- SGLang-JAX stage (formerly the standalone `run_sglang` job) ---------
      # [ORDERING CONSTRAINT]: MUST stay LAST in `run_core`. It installs
      # `sglang-jax` and pins `jax[tpu]<0.11.0` and `qwix`, mutating the Python
      # environment.
      #
      # Gated at the step level rather than the job level so that the unit tests
      # above still run on external fork PRs, where secrets are unavailable.
      # This preserves the previous behaviour, where run_core had no `if:`.
      - name: Run install sglang-jax && test
        if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          ## because sglang-jax has codes like jax.local_devices('cpu')
          # TODO(lancewang): Re-enable this test once the bug is fixed.
          unset JAX_PLATFORMS
          unset PIP_NO_CACHE_DIR
          export PIP_NO_BUILD_ISOLATION=1
          python3 -m pip list | egrep 'jax|flax|libtpu'
          # Constrain jax/jaxlib < 0.11.0 during sglang-jax installation so pip does
          # not upgrade to jax 0.11.1+ and force a subsequent downgrade/reinstall.
          printf "jax<0.11.0\njaxlib<0.11.0\n" > /tmp/sglang_constraints.txt
          export PIP_CONSTRAINT=/tmp/sglang_constraints.txt
          cd ..
          git clone https://github.com/sgl-project/sglang-jax.git && cd sglang-jax && git checkout 1bdbc6747939c0e600622fc7f812151d76321080 && cd python && python3 -m pip install -e . && cd ../..
          # SGLang Jax removed qwix dependency in https://github.com/sgl-project/sglang-jax/pull/734
          python3 -m pip install qwix 'jax[tpu]<0.11.0'
          # TODO(b/470113586): Remove this test once the bug is fixed.
          python3 -m pip list | egrep 'jax|flax|libtpu'

          cd tunix
          python3 -m pytest tests/generate/sglang_jax_sampler_test.py tests/generate/sglang_jax_lora_test.py -v --tb=short
          GITHUB_JOB=run_sglang python3 -m pytest tests/rl/rl_cluster_test.py -v --tb=short -k "sglang_jax"
          rm -rf /root/.cache/huggingface/xet || true

  # ---------------------------------------------------------------------------
  # Job: run_dev
  # Purpose: TPU training/PEFT integration, live vLLM sampler tests, and the
  #          distributed RL smoke test.
  # Hardware: Live TPU v6e-8 accelerator (1 slice).
  # Image:    `us-central1-docker.pkg.dev/cloud-tpu-multipod-dev/tunix/mlperf:<sha>`
  #           (resolved/built by `build_tunix_docker` from `Dockerfile` with Tunix,
  #           vLLM, TPU-Inference, TFDS, and MaxText pre-installed in `/opt/venv`).
  # Secrets:  Requires HF_TOKEN and KAGGLE_* secrets (skipped on external fork PRs).
  # Scope:    End-to-end GRPO RL demo training, vLLM Sampler generation / LoRA
  #           mapping / checkpoint loading, SFT PEFT 2x2 mesh integration, then
  #           the Qwen3-0.6B GSM8K distributed RL smoke test with Raiden sync.
  #
  # ORDERING CONSTRAINTS (within run_dev):
  #   1. `GRPO Integration Test` & `Run vllm tests` run first against `/opt/venv`
  #      where `numpy==2.3.5` is locked after `tensorflow` and `maxtext`.
  #   2. `Run tunix SFT integration tests` runs before the Distributed RL smoke
  #      stage using the pre-installed `tensorflow` and `kagglehub` stack.
  #   3. Distributed RL Smoke Test (`run_dist_smoke` steps) MUST run LAST in this job.
  #      It installs the native Raiden weight-sync wheel (`tpu_sync`) and spawns
  #      multi-process trainer/rollout servers across all 8 TPU chips; running it last
  #      prevents background process or chip-lock leakage into earlier tests.
  # ---------------------------------------------------------------------------
  run_dev:
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: [linux-x86-ct6e-180-8tpu]
    environment: testing
    # Note: GitHub Actions `timeout-minutes` only counts active execution time after
    # a runner picks up the job (queue wait time is excluded). Measured execution
    # is ~20.9m, so 60m provides ~3x headroom while catching hung processes.
    timeout-minutes: 60
    container:
      image: ${{ inputs.dev_image || 'us-central1-docker.pkg.dev/cloud-tpu-multipod-dev/tunix/mlperf:latest' }} # zizmor: ignore[unpinned-images]
      options: --privileged
      env:
        CLOUD_TPU_ACCELERATOR: v6e-8
        JAX_PLATFORMS: tpu,cpu
    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Cache TFDS datasets
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: /root/.cache/tensorflow_datasets
          key: tfds-dev-v4-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'scripts/install_*.sh') }}
          restore-keys: |
            tfds-dev-v4-${{ runner.os }}-
            tfds-dev-v3-${{ runner.os }}-

      - name: Cache JAX compilation
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: /root/.cache/jax_compilation_cache
          key: jax-cache-dev-v4-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'scripts/install_*.sh') }}
          restore-keys: |
            jax-cache-dev-v4-${{ runner.os }}-
            jax-cache-dev-v3-${{ runner.os }}-

      # Cache model weights (`Qwen2.5-0.5B` for GRPO, `gemma2-2b-it` in Kagglehub
      # for SFT, `Qwen3-0.6B` in `ARTIFACT_ROOT/models`) and the pre-converted
      # MaxText Orbax checkpoint (`ARTIFACT_ROOT/maxtext_models`, which skips
      # `convert_hf_to_maxtext` on warm runs) without baking any data into the
      # `tunix/mlperf` Docker image.
      #
      # Why `xet/` and `models--Qwen--Qwen2.5-7B*` are excluded:
      #   - `HF_XET_HIGH_PERFORMANCE=1` streams `Qwen2.5-7B` from the Hugging Face
      #     Xet CDN in ~7s, whereas archiving and restoring its 15 GB of uncompressible
      #     bfloat16 safetensors via `actions/cache` adds ~50s of tar/zstd restore
      #     overhead and consumes ~6.2 GB of GitHub's 10 GB repository cache limit.
      #   - `xet/` holds temporary staging chunks written alongside `hub/`.
      - name: Cache Hugging Face models and MaxText checkpoints
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: |
            /root/.cache/huggingface
            !/root/.cache/huggingface/xet
            !/root/.cache/huggingface/hub/models--Qwen--Qwen2.5-7B*
            /root/.cache/kagglehub
            /root/.cache/tunix_artifacts/qwen3_dist_gsm8k/models
            /root/.cache/tunix_artifacts/qwen3_dist_gsm8k/maxtext_models
          key: hf-maxtext-dev-v3-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            hf-maxtext-dev-v3-${{ runner.os }}-

      # `tunix/mlperf` already has all runtime, vLLM, TPU-Inference, and MaxText
      # dependencies pre-installed and verified in `/opt/venv`. Running
      # `uv pip install -e ".[tpu,cli,test,experimental]"` links the current
      # checkout in ~1s when all dependencies are already satisfied, while
      # automatically installing any new dependency if run against an older image.
      - name: Setup Tunix , tpu-inference and dependencies
        run: |
          uv pip install -e ".[tpu,cli,test,experimental]"

      - name: GRPO Integration Test (Llama3 / Qwen2 multi-device RL training demo)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # Run GRPO demo script with minimal configuration
          python3 scripts/grpo_demo_llama3_qwen2.py \
            --root-dir=/tmp/grpo_test \
            --num-batches=2 \
            --num-test-batches=1 \
            --global-batch-size=2 \
            --train-mini-batch-size=2 \
            --train-micro-batch-size=2 \
            --rollout-engine=vanilla

      # Combine `VllmSamplerConfigTest` and `vllm_sampler_qwen_test.py` into a
      # single `pytest` invocation to avoid paying Python/JAX/vLLM interpreter
      # startup and PJRT TPU initialization overhead twice.
      - name: Run vllm tests
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          unset JAX_PLATFORMS
          python3 -m pytest tests/generate/vllm_sampler_test.py::VllmSamplerConfigTest tests/generate/vllm_sampler_qwen_test.py -v --tb=short
          python3 -m pytest tests/generate/vllm_sampler_test.py::VllmSamplerTest -k "batch_mode and not data_parallel or sampling_kwargs" -v --tb=short
          python3 -m pytest tests/generate/vllm_sampler_test.py::VllmSamplerTest -k "server_mode or concurrency" -v --tb=short
          python3 -m pytest tests/generate/vllm_sampler_test.py::VllmSamplerTest -k "data_parallel" -v --tb=short
          rm -rf /root/.cache/huggingface/xet /root/.cache/huggingface/hub/models--Qwen--Qwen2.5-7B* || true

      - name: Run tunix SFT integration tests (PEFT with 2x2 mesh on TPU)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          export JAX_PLATFORMS=tpu,cpu
          ./tests/sft/sft_tpu_smoke_test.sh

      # --- Distributed RL smoke stage (formerly the standalone `run_dist_smoke`
      # job) --------------------------------------------------------------------
      # [ORDERING CONSTRAINT]: MUST stay LAST in `run_dev`. `install_raiden.sh`
      # installs a pinned native weight-sync wheel (`tpu_sync`), and the recipe
      # below spawns multi-process trainer / rollout servers across all 8 TPU chips.
      # Running it last ensures no background processes or chip locks affect
      # earlier single-process tests.
      - name: Install Raiden weight sync library
        run: bash scripts/install_raiden.sh

      - name: Verify TPU runtime preflight
        run: |
          # Dump the vfio-ID -> PCI-address map the chip split relies on.
          python3 scripts/get_sorted_tpu_ids.py --human

      # Point `ARTIFACT_ROOT` to the cached `/root/.cache/tunix_artifacts/...`
      # directory so `Qwen/Qwen3-0.6B` (`models/`) and its converted MaxText
      # Orbax checkpoint (`maxtext_models/`) persist across CI runs while
      # keeping ephemeral training outputs (`CHECKPOINT_ROOT_DIRECTORY` and
      # `MAXTEXT_OUTPUT_DIR`) in `/tmp`. `WAIT_POLL_SECS=1` and
      # `WORKER_SHUTDOWN_GRACE_SECS=2` tighten local process polling/teardown.
      - name: Run GSM8K Distributed RL Smoke Test (Tunix Backend + Raiden Sync)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          ARTIFACT_ROOT: /root/.cache/tunix_artifacts/qwen3_dist_gsm8k
          CHECKPOINT_ROOT_DIRECTORY: /tmp/qwen3_dist_gsm8k/checkpoints
          MAXTEXT_OUTPUT_DIR: /tmp/qwen3_dist_gsm8k/maxtext_out
          WAIT_POLL_SECS: "1"
          WORKER_SHUTDOWN_GRACE_SECS: "2"
        run: |
          ./tunix/experimental/examples/recipes/ci_smoke_gsm8k_qwen3_0p6b.sh

      - name: Run GSM8K Distributed RL Smoke Test (MaxText Backend + Raiden Sync)
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          ARTIFACT_ROOT: /root/.cache/tunix_artifacts/qwen3_dist_gsm8k
          CHECKPOINT_ROOT_DIRECTORY: /tmp/qwen3_dist_gsm8k/checkpoints_maxtext
          MAXTEXT_OUTPUT_DIR: /tmp/qwen3_dist_gsm8k/maxtext_out
          WAIT_POLL_SECS: "1"
          WORKER_SHUTDOWN_GRACE_SECS: "2"
        run: |
          ./tunix/experimental/examples/recipes/ci_smoke_gsm8k_qwen3_0p6b_maxtext.sh
          rm -rf /root/.cache/huggingface/xet || true

      # Fires for any failure in this job, not just the smoke test above; the
      # `|| true` guards make that harmless when the logs do not exist.
      - name: Print logs on failure
        if: ${{ failure() }}
        run: |
          echo "==================== TRAINER LOG ===================="
          cat tunix/experimental/examples/math_gsm8k_dist/trainer.log || true
          echo "==================== ROLLOUT LOG ===================="
          cat tunix/experimental/examples/math_gsm8k_dist/rollout.log || true
          echo "==================== ORCHESTRATOR LOG ===================="
          cat tunix/experimental/examples/math_gsm8k_dist/orchestrator.log || true
