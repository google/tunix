# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; finite nonzero A-B is observed; B-C remains exact; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the requested pull fast-forwarded the isolated P58 worktree to `609c8e6d6d2cb9e7ebd0ea8fa0d7a4fe0b877f68`, which adds only immutable `p58f04` evidence. The arm-aware live-weight repair worked on target: `[P34.WEIGHTS] EXACT` covered 398 leaves and 4,022,468,096 elements across the 64-device rollout role. A local, unpublished repair now separates the native observer-only processed-B path from the deferred zero canonical engine. `main` remains untouched; no commit or push is authorized by this checkpoint.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f04` completed 128 durable trajectories in 557.2 seconds: 125 `SUCCEEDED`, three `MAX_CONTEXT_LIMIT_REACHED`, six solved trajectories, five all-failed groups, one mixed/effective group, two incomplete groups, and 16 nonzero advantages. Sandbox-start timeouts were zero. Exact live weights then passed. The first failure was before trainer forward/backward/update: shared RLCluster requested processed `S_prefill`, but native correctly kept `CANON_PROMPT_PROCESSED_LOGPROBS=0`, and the rollout contract rejected labeling the stock raw helper as processed.
- Local repair: native still keeps every zero-TIM numerical switch disabled/absent. It alone enables `CANON_P58_NATIVE_STOCK_PROMPT_OBSERVER=1`, which installs a separately hashed two-file overlay after proving the engine stock. The overlay changes only post-rollout B observation, applies decode-equivalent temperature/top-k/top-p transforms, and builds targets from absolute request history rather than rolling across DP-packed/request boundaries. Zero sets this flag to `0` and continues through `CANON_PROMPT_PROCESSED_LOGPROBS=1`, `CANON_ENGINE_MODULE_C=1`, and the complete canonical bundle. Profile, shell, Python, installer, and postflight contracts reject mixed treatments.
- Validation: P58 profile/environment/static tests pass; P57 adjacency passes 91/91; P34 static passes 10 suites; exact patch application and the full pinned image pass with `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. The pinned probe proves absolute target identity and processed values. Host-only direct Tunix imports lack `metrax`; those same tests pass in the pinned image.
- Next action: after explicit commit/push approval, publish and read back this repair, then render only `arm=native, stage=full` with fresh run-id `p58f05`. Require stock preflight, exactly one `[P58.STOCK_OBSERVER] PROCESSED_PROMPT_LOGPROBS_PASS`, `[P34.WEIGHTS] EXACT`, finite forward/backward, and the first optimizer commit before treating the boundary as repaired. Continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f04 has no trainer forward, backward, optimizer commit, or checkpoint; it is immutable `INCONCLUSIVE` and is not resumable training. P58c01 through p58c05 and p58f01 through p58f04 remain attempt evidence. Zero launch remains deferred.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `evidence/p58f04/run.log` (SHA-256 `a7b0cda5e7d359c7e320b29f8af197db0dd6c46dc34850aa55ffb350fb766fdd`); p58f04 trajectory journal `/mnt/disks/linchai_data/deepswe_zero_tim/canon-p58-ds4b-native-full-p58f04/debug/batch-000000.trajectories.jsonl.gz` (SHA-256 `e39caf5df63ba54406a36427a413dea562e5771f4c52b30c840229d3178c1f3b`); prior immutable evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
