# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; at least one finite nonzero Native serving-path mismatch is observed across A-B or B-C; every Native numerical boundary is shape-valid and finite; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: source intake fast-forwarded the isolated P58 worktree to exact operator tip `28817bfb3a14c95f42b3950f03380d1c6c03d336`, which adds immutable p58f10 timeout evidence. The one-wave concurrency repair is local and uncommitted. No zero-TIM flag was enabled, deleted, or repurposed; `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f10` used the correct 128-chip Native B8 x G16 recipe and entered Step-0 rollout, but its 128 trajectories were throttled by `max_concurrency=64` into two sequential waves. At the unchanged 3,600-second rollout-batch deadline only 5/8 prompt groups were complete; the orchestrator raised `TimeoutError` and exited before the P58 journal, trainer forward/backward, optimizer receipt, or checkpoint. This is rollout scheduling geometry, not a model, reward, loss, Native mismatch, or optimizer failure.
- Local repair: keep B8 x G16 and all 128 trajectories, but set `max_concurrency=128`, equal to both the raw batch and the provisioned rollout capacity `DP8 x rollout_vllm_max_num_seqs16`. The batch is therefore one concurrency wave. Preserve episode 3,000 s, cleanup 300 s, and batch 3,600 s; individual timeout/context outcomes remain compact journal rows with zero policy masks, while failure to drain the one-wave batch by the hard deadline remains fail-closed. All Native/Zero flags, numerical programs, roles, topology, model/data, loss, optimizer placement/math, and 1,000-commit horizon are unchanged.
- Validation: Python compilation and `git diff --check` pass. Host renderer passes 15/15, including a negative geometry control requiring raw trajectories = max concurrency = rollout DP x vLLM sequence capacity = 128 and batch deadline > episode + cleanup. A fresh p58f11 render emits `P58_DEEPSWE_TIM_RENDER_PASS` and contains B8, G16, concurrency 128, rollout DP8 x max-seqs16, and the unchanged deadlines. P34 static emits `P34_STATIC_PASS suites=10`; P57 adjacency passes 105/105 with `P57_FROZENLAKE_TIM_CPU_PASS`. Pinned image `sha256:418dc632edd8ff990e8880df6a5ca82369f6c4d705e16152c1ee6f9708d5e53a` emits `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. These gates do not claim a new Kubernetes or TPU PASS.
- Next action: after separate commit/push approval, publish and exactly read back `yuxzhang/canon-zero-tim`, then use fresh native full run-id `p58f11`. Before apply require the one-wave equality above plus `cpu-np`, retained head host networking, required head anti-affinity, JobSet DNS, host-network TPU workers, and exact RM/PATHWAYS_HEAD routing. Require a durable 128-row journal including compact rows, all Native boundaries and ratios finite, a nonzero serving-path treatment dose, finite backward, a valid TPU-resident optimizer receipt, and the first commit; continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f08 through p58f10 have no optimizer receipt or checkpoint and are immutable `INCONCLUSIVE`; none has resumable training state. P58c01 through p58c05 and p58f01 through p58f10 remain attempt evidence. The local repair needs publication and a fresh Kubernetes execution. Zero launch remains deferred. Direct one-host TPU execution remains blocked by unavailable direct TPU device exposure and cannot validate multi-JobSet CPU-node placement.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `../../debug_logs/p58_p58f10_deepswe_batch_timeout.raw.log`; `../../debug_logs/p58_p58f10_deepswe_batch_timeout.classification.json`; `evidence/p58f09/run.log` (SHA-256 `8977eefcb2ef34bc17c4dbb6e129b1d02cacba6b63041ab42d43a3aa8b5f4d0b`); prior immutable evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
