# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; finite nonzero A-B is observed; B-C remains exact; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the requested pull fast-forwarded the isolated P58 worktree to `606b37cf4984a22bcb46391c18834a1006bfb98b`, which adds immutable `p58f01` evidence. The run used source `6f18d95b22835fc70326d21bb70c1fb41f7b0e12`. The worktree contains a local, uncommitted repair for the two observed boundaries; `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f01` passed JobSet admission, exact 128-device Pathways discovery, the 64/64 role split, Qwen3-4B/vLLM and W&B initialization, and entered the rollout producer at concurrency 64. Every sandbox reset then timed out: the bounded log contains 128 `ENV_RESET_TIMEOUT` rows and at least 127 Pod markers whose scheduler condition is `SchedulingGated`. The standalone R2E Pods had no `kueue.x-k8s.io/queue-name`, so Kueue gated them without a LocalQueue. The completed all-timeout batch then exposed a secondary local bug: reset failed before the first model call assigned `policy_version`, and `_process_results` rejected the missing provenance before the journal boundary.
- Next action: publish/read back the repair only after explicit approval, then render only `arm=native, stage=full` with fresh run-id `p58f02`. Require every R2E Pod to inherit parent queue `multislice-queue`; verify its Kueue Workload is admitted and the scheduling gate is removed before diagnosing model throughput. Monitor commits 1–3 without stopping a healthy job; full PASS still requires exactly 1,000 commits. Do not render/apply zero.
- Blockers: no completed real R2E trajectory, forward, backward, optimizer commit, or checkpoint evidence exists. P58c01 through p58c05 and p58f01 are `INCONCLUSIVE`, not training results; p58f01 crashed before durable batch persistence and has no resumable journal state. This host has no `libtpu.so`, so a real one-host v5p validation cannot run here. Zero launch remains deferred.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-4-native-canary.md`; `phases/p58-5-native-full.md`; `evidence/p58f01/run.log` (SHA-256 `16c513c773ac2bfb1542178b4e42b03098bb9114564106b03f83c0195a0d542f`); prior immutable attempt evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-21 UTC
