# State

- Status: active
- Objective: first establish that the untreated native DeepSWE-derived Qwen3-4B-Instruct path can complete a signed 128-chip three-update canary on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly three optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, and checkpoint evidence is durable; finite nonzero A-B is observed; B-C remains exact; and the native classifier says `PASS`. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the admission fix and regression were published to `yuxzhang/canon-zero-tim` as `acd3136267214b367a6755d0ba28d80e883d6753`; the first post-push readback matched exactly with ahead/behind `0/0`. This later documentation-only checkpoint will advance the branch once more, so executors must fetch and use the final remote SHA. `main` remains untouched.
- Current phase: P58.4N — native 128-chip three-update canary.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS.
- Last verified fact: p58c01 Attempt-0 exited in `00_env.sh` before any Pathways device probe, model, rollout, trajectory, backward, or optimizer action. The first failing contract had three missing explicit FrozenLake-zero flags and an incorrect inherited demand that native claim `CANON_P32_DP_REDUCTION_ADMITTED=1`. The local fix keeps truthful stock reduction admission at `0`, exports the three unrelated zeros, passes a real renderer-to-`00_env.sh` regression, and emits `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 regressions=1`.
- Next action: after separate commit/push approval, the remote executor pulls the read-back fix SHA, supplies a digest-pinned image, renders only `arm=native, stage=three-update` with fresh run-id `p58c02`, performs server-side dry-run, and launches it. Do not reuse p58c01 and do not render/apply zero.
- Blockers: no p58c02 digest image or 128-chip canary evidence exists. P58c01 is bootstrap `INCONCLUSIVE`, not a training result. Zero launch remains deferred.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-4-native-canary.md`; `evidence/p58c01/run.log` (SHA-256 `f551712696c9c36dbf4f1f2fb713a4c975ff49f2184cf62e887341679341d0bc`); `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-21 UTC
