# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; at least one finite nonzero Native serving-path mismatch is observed across A-B or B-C; every Native numerical boundary is shape-valid and finite; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: source intake first fast-forwarded through immutable p58f07 evidence, then reconciled one later non-overlapping P57-only alignment-policy commit. The superseded trainer-rescore geometry change was removed before publication. The signed Native warning-policy/classifier repair was published as `81622977bf15393798c671e578ee059d1268e78b`; its first readback matched local HEAD, `FETCH_HEAD`, and `origin/yuxzhang/canon-zero-tim` with ahead/behind `0/0`. This documentation checkpoint advances the branch once more, so executors must fetch the final operator tip. No zero-TIM flag was enabled, deleted, or repurposed; `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f07` completed all 128 real SWE RepoEnv trajectories with `N_action=436,464`. Pre-backward alignment passed with finite Native A-B/B-C warnings (`830,053` and `1,169,723` differing bytes), proving the p58f06 policy repair. Rescore B completed in 26.9 seconds and the first real value-and-grad/backward call ran, then the post-backward gate stopped on `T_old_vs_T_current` and derived `r_all_exactly_1`. The durable p58f06 marker proves the standalone trainer observer used one 128-trajectory program, while training uses eight 16-trajectory value-and-grad programs. The gate therefore compared different batch programs rather than a same-program repeat.
- Local repair: signed P58 Native now treats finite `T_old_vs_T_current` and its finite derived `r`/`w*r` diagnostics as warnings, alongside the existing finite A-B/B-C warnings. This preserves the complete stock `yuxzhang/deepswe-quality-fix` numerical programs and the stock 128-trajectory standalone observer. With `use_rollout_logps=true` and sampler-IS disabled, training uses rollout A as `old_per_token_logps`; `T_old` is observer-only and cannot veto an otherwise finite backward/update. The classifier requires this trainer-program boundary to be present, shape-valid, and finite, but not exact. Zero remains strict at every boundary. Nonfinite values, invalid shapes, weights, replicas, transactions, optimizer failures, OOM, and corrupt artifacts remain hard.
- Validation: Python compilation, flag-registry audit (`320/320`, `FLAG_AUDIT_PASS`), and `git diff --check` pass. Host P58 environment passes 5/5, profile 2/2, renderer 7/7, and alignment policy 9/9. P34 static emits `P34_STATIC_PASS suites=10`; current P57 adjacency passes 105/105 and emits `P57_FROZENLAKE_TIM_CPU_PASS`. In pinned image `sha256:418dc632edd8ff990e8880df6a5ca82369f6c4d705e16152c1ee6f9708d5e53a`, classifier 5/5, shared alignment 42/42, P34/P44 neighbors, stock-observer probes, and the complete gate pass with `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. The existing bounded one-host runner is still blocked because this container exposes no `/dev/vfio`; no TPU result is claimed.
- Next action: fetch and exactly read back the final operator tip, then use fresh native full run-id `p58f08`. Require all Native boundaries and ratios to be finite, a nonzero serving-path treatment dose, finite backward, a valid TPU-resident optimizer receipt, and the first commit before promoting the boundary; continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f07 has no durable optimizer receipt or checkpoint; it is immutable `INCONCLUSIVE` and not resumable training state. P58c01 through p58c05 and p58f01 through p58f07 remain attempt evidence. Zero launch remains deferred. Direct one-host TPU execution remains blocked by unavailable direct TPU device exposure.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `evidence/p58f07/run.log` (SHA-256 `147332c0d9ffc6a4e5016963b18f427efeee683adb2a31defcd671941a1c58ef`); p58f07 target alignment report `/mnt/disks/linchai_data/deepswe_zero_tim/canon-p58-ds4b-native-full-p58f07/alignment.jsonl` (named by the target traceback but not copied into this repository); prior immutable evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
