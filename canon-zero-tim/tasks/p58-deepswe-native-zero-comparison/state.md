# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; at least one finite nonzero Native serving-path mismatch is observed across A-B or B-C; trainer-old/current remains exact; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the isolated worktree is based on operator tip `68fa7d924ef7138e99cc2864ebbcf9edb6e676d9`, which contains immutable p58f06 evidence plus later P57 evidence and execution-log checkpoints. A local correction approved for publication restores the Native warning scope to both finite serving-path boundaries without enabling, deleting, or repurposing a zero-TIM flag. `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f06` completed a 492.7-second rollout and durably wrote 128 trajectories: 126 `SUCCEEDED`, two `MAX_CONTEXT_LIMIT_REACHED`, three solved, five all-failed groups, one mixed/effective group, two incomplete groups, and 31 effective nonzero advantages. All timeout dimensions were zero. Exact live weights passed for 398 leaves and 4,022,468,096 elements; the Native processed-B observer passed all 2,048 prompt rows. The full-stage admission repair worked and alignment executed over 405,827 action tokens. Both A-B and B-C were shape-valid and finite, but B-C was still outside P58's narrowed warning tuple, so the run stopped before trainer forward/backward/update with `S_prefill_vs_T_old` blocking.
- Local repair: signed P58 Native now treats finite `S_decode_vs_S_prefill` and finite `S_prefill_vs_T_old` differences as durable treatment warnings. This matches the untreated serving/trainer comparison and the user's earlier decision that finite B-C must not stop Native training. The trainer-internal `T_old_vs_T_current` repeat boundary and derived `r` remain exact/fail-closed; nonfinite, invalid shape, weight, replica, transaction, optimizer, and all Zero-arm differences remain hard. The P58 classifier accepts a finite nonzero dose on either Native serving boundary and separately requires exact trainer repeat. No new flag was added and every Native/Zero numerical switch remains unchanged.
- Validation: host-direct profile 2/2, renderer 7/7, alignment policy 8/8, environment 5/5, and classifier 4/4 pass. P34 static passes all 10 suites; current P57 adjacency passes 105/105 with `P57_FROZENLAKE_TIM_CPU_PASS`; and the shared alignment regression passes 40/40. Python compilation, flag-registry audit, shell syntax, and `git diff --check` pass. The complete pinned image passes with `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. The bounded one-host runner is implemented and syntax-checked, but this container exposes no `/dev/vfio` and reports zero TPU chips; its actual result is `P58_ONEHOST_ALIGNMENT_BLOCKED reason=device_inventory_timeout`, not PASS.
- Next action: commit and push only to `yuxzhang/canon-zero-tim`, verify exact remote readback, then use fresh native full run-id `p58f07`. Require both finite mismatch boundaries to be warnings, finite forward/backward, exact trainer repeat, and the first optimizer commit before promoting the repaired boundary; continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f06 has no trainer forward, backward, optimizer commit, or checkpoint; it is immutable `INCONCLUSIVE` and is not resumable training. P58c01 through p58c05 and p58f01 through p58f06 remain attempt evidence. Zero launch remains deferred. Direct one-host TPU execution is currently blocked by unavailable TPU metadata in this shell/container.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `evidence/p58f06/run.log` (SHA-256 `34c6830d5b4179cf8ccdd697a0b03d9764fc75ffefa9313d5a1910914e774fd9`); p58f06 trajectory journal `/mnt/disks/linchai_data/deepswe_zero_tim/canon-p58-ds4b-native-full-p58f06/debug/batch-000000.trajectories.jsonl.gz` (SHA-256 `ddaefb3c0efc8eb7f29724c80b5aa88ab38e8b49e7bd3cf7134c4916afe2e6f3`); prior immutable evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
