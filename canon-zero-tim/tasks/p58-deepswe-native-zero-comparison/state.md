# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; finite nonzero A-B is observed; B-C remains exact; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the requested pull fast-forwarded the isolated P58 worktree to `5dd865294560899b0438228f458a84acbe61cdb4`, which records immutable `p58f03` evidence. The run used source `7208d7b330759ac7dc31493ece65d32a6c355308`. The user correctly resolved the preceding CPU scheduling failure by moving the head and sandboxes to `cpu-np`; no general CPU fallback belongs in the training code. The abandoned local fallback remains recoverable as `stash@{0}` and is absent from this tree. `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f03` completed the first real rollout batch in 616.3 seconds: 128 durable trajectory rows, 126 `SUCCEEDED`, two `MAX_CONTEXT_LIMIT_REACHED`, three solved trajectories, two mixed/effective prompt groups, and 32 nonzero advantages. Sandbox-start timeout count was zero. It then failed before trainer forward/backward/update because the shared P34 weight gate called `attest_canonical_engine_weights`, which intentionally does not exist when native P58 keeps `CANON_ENGINE_MODULE_C=0`. The first error was `canonical weight attestation requires the registered engine adapter`; the following alignment-gate error was derivative.
- Local repair: the shared weight gate now calls an arm-aware exact-live-weight interface. Zero still delegates to its registered canonical adapter. Signed P58 native uses the same pure trainer-to-engine leaf mapping and bitwise comparison as a read-only observer, without constructing/registering the canonical adapter or replacing serving functions. Missing/mismatched weights, invalid DP8 x TP8 mesh, a leaked native adapter, or an unsigned route remain hard errors. The public attestation receipt normalizes the internal vLLM `data/model` axes to contract `dp/tp` names.
- Validation: focused native/zero/negative routing tests and live-weight observer tests pass; all 15 rollout canonical tests pass; and the full pinned P58 exact-image gate exits zero with `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 regressions=1`. A wider legacy adapter test invocation had three unrelated environment/device setup errors (missing active-workload environment or four devices), so it is not claimed as a whole-suite PASS.
- Next action: after explicit commit/push approval, publish the arm-aware weight-attestation repair and read back the final operator SHA. Then render only `arm=native, stage=full` with fresh run-id `p58f04`. Require `[P34.WEIGHTS] EXACT` before A/B/C, then monitor commits 1–3 without stopping a healthy job; full PASS still requires exactly 1,000 commits. Do not render/apply zero.
- Blockers: p58f03 proves real rollout and trajectory durability but has no trainer forward, backward, optimizer commit, or checkpoint; it is `INCONCLUSIVE` and is not resumable training. P58c01 through p58c05 and p58f01 through p58f03 remain immutable attempt evidence. This host has no `libtpu.so`, so a real one-host v5p validation cannot run here. Zero launch remains deferred.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-4-native-canary.md`; `phases/p58-5-native-full.md`; `evidence/p58f03/run.log` (SHA-256 `fdb958d5e1db8bafa25b6df8c3223a3c6a642d00c6a1915bb34a8e17b5bcf600`); p58f03 trajectory journal `/mnt/disks/linchai_data/deepswe_zero_tim/canon-p58-ds4b-native-full-p58f03/debug/batch-000000.trajectories.jsonl.gz` (SHA-256 `26c92d2153865cc14296303fcb97afd98f857744e50574032b6eba8631f23a9e`); prior immutable attempt evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-21 UTC
