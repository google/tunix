# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; finite nonzero A-B is observed; B-C remains exact; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: P58 evidence commit `af4975c42ad9dd49799973027f5ae7b4677ac362` adds immutable `p58f05`; the following tip is a P57-only change. The P58 native full-stage warning-policy repair was published as `5132d7ad0d3bc7c53de09e20bae835dca18a211a`; its post-push readback matched local HEAD and the remote-tracking operator branch with ahead/behind `0/0`. It does not enable or delete any zero-TIM numerical flag. `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f05` completed 128 durable trajectories in 486.4 seconds: 126 `SUCCEEDED`, two `MAX_CONTEXT_LIMIT_REACHED`, six solved trajectories, five all-failed groups, two mixed/effective groups, one incomplete group, and 32 nonzero advantages. Sandbox-start, environment, model, and reward timeouts were zero. Exact live weights passed for 398 leaves and 4,022,468,096 elements, and the independent native processed-B observer passed for all 2,048 prompt rows. The first failure was before trainer forward/backward/update: `gsm8k_ab_report_policy()` recognized the P58 arm but admitted P58 only through its obsolete short-update branch, so the signed `stage=full, expected_updates=1000` tuple was incorrectly rejected.
- Local repair: the DeepSWE warning policy now distinguishes production P34 full, registered P39/P43/P44 debug updates, and signed P58 native training. P58 requires `CANON_P58_TIM_ADMITTED=1`, no competing DeepSWE mode, and an exact stage/horizon pair (`three-update/3` or `full/1000`). Native continues to warn only for finite `S_decode_vs_S_prefill`; B-C, nonfinite, shape, replica, weight, transaction, and optimizer failures remain hard. Zero keeps `CANON_DEEPSWE_ALIGNMENT_WARN_ONLY=0` and strict A=B=C. No new flag was added and every Native zero-TIM disable/absence remains intact.
- Validation: host-direct alignment policy passes 5/5 and renderer-to-profile/environment passes 5/5, including a real rendered Native full environment and admission/horizon negative controls. Profile passes 2/2, renderer 7/7, and adjacent P34 warning 3/3. P34 static passes all 10 suites and current P57 adjacency passes 102/102 with `P57_FROZENLAKE_TIM_CPU_PASS`. Python compilation, registry audit, shell syntax, and `git diff --check` pass. The complete pinned image passes with `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. The bounded one-host runner is implemented and syntax-checked, but this container exposes neither TPU metadata nor `/dev/vfio`; its actual result is `P58_ONEHOST_ALIGNMENT_BLOCKED reason=device_inventory_timeout`, not PASS.
- Next action: an executor must fetch and read back `yuxzhang/canon-zero-tim`, then use fresh native full run-id `p58f06`. If four direct TPU devices become visible before launch, execute the same one-host gate and preserve its PASS marker; otherwise retain `BLOCKED_DIRECT_TPU_METADATA`. Require finite forward/backward and the first optimizer commit before promoting the repaired boundary; continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f05 has no trainer forward, backward, optimizer commit, or checkpoint; it is immutable `INCONCLUSIVE` and is not resumable training. P58c01 through p58c05 and p58f01 through p58f05 remain attempt evidence. Zero launch remains deferred. Direct one-host TPU execution is currently blocked by unavailable TPU metadata in this shell/container.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `evidence/p58f05/run.log` (SHA-256 `73def19531ca1a9ef083a30d11ceb89696afcbe4125bd128f7ff0e7152ec06a6`); p58f05 trajectory journal `/mnt/disks/linchai_data/deepswe_zero_tim/canon-p58-ds4b-native-full-p58f05/debug/batch-000000.trajectories.jsonl.gz` (SHA-256 `90c179d799bb97416f1a4e6cf944a15326cef56360da179c771fad79fa02bcac`); prior immutable evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
