# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; at least one finite nonzero Native serving-path mismatch is observed across A-B or B-C; every Native numerical boundary is shape-valid and finite; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: source intake fast-forwarded the clean isolated P58 worktree to operator tip `3edf480072126145acc2df259419e12dd2737c69`, which adds the completed p58f08 infrastructure diagnosis and immutable p58f09 log. The P58 head-placement and trajectory-input repairs are local and uncommitted pending separate publication approval. No zero-TIM flag was enabled, deleted, or repurposed; `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f09` proved the `cpu-np` head placement and correct Pathways attachment, then completed all 128 Step-0 rollout slots in 1,699.1 seconds on the signed B8 x G16 / DP8 x TP8 plus DP8 x TP8 recipe. Several environment resets ended at the admitted 3,000-second trajectory deadline and one trajectory hit `MAX_CONTEXT_LIMIT_REACHED`. The collector nevertheless reported 128 execution and 128 observed trajectories. Learner preprocessing then called `merge_micro_batches()` with one or more `original_input=None` entries and crashed with `AttributeError: 'NoneType' object has no attribute 'keys'` before the P58 journal, alignment, forward, backward, optimizer receipt, or checkpoint.
- Local repair: preserve the proven Pathways head transport (`hostNetwork:true`, `ClusterFirstWithHostNet`) and keep `nodeSelector: cpu-np`; do not use `deepswe-cpu-pool`, whose cross-subnet scheduler pipe failed in p58f08. Add required hostname-level Pod anti-affinity over every JobSet `pathways-head` replicated-job Pod so a new head cannot share fixed ports 29000/29001 with another head and the autoscaler must provision an unused CPU node. At trajectory construction, prefer the agent's observed task but fall back to the environment's original task when reset terminates before the first observation; fail closed if neither is a dictionary. Compact timeout/context rows remain journaled and zero-masked under the existing recipe; they are not dropped or resampled. B8 x G16, all Native/Zero flags, numerical programs, roles, topology, model/data, deadlines, loss, optimizer placement/math, and commit horizon are unchanged.
- Validation: Python compilation and `git diff --check` pass. Host P58 renderer passes 14/14, profile 2/2, environment 5/5, and alignment policy 9/9; the negative control rejects `deepswe-cpu-pool`. A fresh p58f10 render contains `cpu-np`, retained head/worker host networking, the exact required JobSet-head hostname anti-affinity, JobSet DNS publication, and exact RM/PATHWAYS_HEAD route. P34 static emits `P34_STATIC_PASS suites=10`; P57 adjacency passes 105/105 with `P57_FROZENLAKE_TIM_CPU_PASS`; the flag registry passes 320/320 with `FLAG_AUDIT_PASS` and `changed_names=0`. Pinned image `sha256:418dc632edd8ff990e8880df6a5ca82369f6c4d705e16152c1ee6f9708d5e53a` passes both new trajectory-input controls and emits `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. These local gates do not claim a new Kubernetes or TPU PASS.
- Next action: after validation, separate commit/push approval, publication, and exact remote readback, use fresh native full run-id `p58f10`. Before apply require `cpu-np`, retained head host networking, exact required head anti-affinity, JobSet DNS, host-network TPU workers, and exact RM/PATHWAYS_HEAD routing. Require stock preflight, correct Pathways CL attachment, a durable 128-row journal including compact rows, all Native boundaries and ratios finite, a nonzero serving-path treatment dose, finite backward, a valid TPU-resident optimizer receipt, and the first commit; continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f08 and p58f09 have no optimizer receipt or checkpoint and are immutable `INCONCLUSIVE`; neither has resumable training state. P58c01 through p58c05 and p58f01 through p58f09 remain attempt evidence. The anti-affinity/original-input repair still needs publication and a fresh Kubernetes execution. Zero launch remains deferred. Direct one-host TPU execution remains blocked by unavailable direct TPU device exposure and cannot validate multi-JobSet CPU-node placement.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `evidence/p58f08/run.log`; `evidence/p58f09/run.log` (SHA-256 `8977eefcb2ef34bc17c4dbb6e129b1d02cacba6b63041ab42d43a3aa8b5f4d0b`); prior immutable p58f03–p58f07 trajectory/alignment evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
