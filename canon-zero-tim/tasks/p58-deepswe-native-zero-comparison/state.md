# State

- Status: active
- Objective: first establish that the untreated native DeepSWE-derived Qwen3-4B-Instruct path can complete a signed 128-chip three-update canary on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly three optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, and checkpoint evidence is durable; finite nonzero A-B is observed; B-C remains exact; and the native classifier says `PASS`. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the implementation was rebased without conflict over the two unrelated P57 commits and published to `yuxzhang/canon-zero-tim` as implementation commit `c5bdc9d993dfaf1a6956335609fbf259f9ed95f7`. The first post-push readback matched exactly with ahead/behind `0/0`. Executors must still obtain the latest runnable source SHA by their own remote readback because this publication-evidence note is a later documentation-only commit. No image publication, JobSet apply, Kubernetes resource, or TPU execution exists yet.
- Current phase: P58.4N — native 128-chip three-update canary.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS.
- Last verified fact: after the rebase, the pinned image `sha256:418dc632edd8ff990e8880df6a5ca82369f6c4d705e16152c1ee6f9708d5e53a` emitted `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 regressions=1`; renderer 4/4, profile 2/2, and environment 1/1 also passed. The gate does not prove a real model rollout, HBM, Pathways, native mismatch dose, or zero exactness on TPU.
- Next action: the remote executor pulls the read-back operator SHA, reruns the pinned-image gate, supplies a digest-pinned client image, renders only `arm=native, stage=three-update`, performs server-side dry-run, and launches the native JobSet. Do not render or apply zero in this phase.
- Blockers: no digest-published client image or 128-chip native canary evidence exists. The missing one-host run is an explicit waiver, not a blocker and not evidence. Zero launch is intentionally deferred pending optimization and renewed user approval.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-1-loss-aggregation-contract.md`; `phases/p58-4-native-canary.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-21 UTC
