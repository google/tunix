# State

- Status: active
- Objective: run the untreated native DeepSWE-derived Qwen3-4B-Instruct path as a full 1,000-update 128-chip campaign on the frozen clean-data recipe. Preserve the zero arm for the eventual causal comparison, but do not launch it until its optimization work is complete and the user explicitly reactivates it.
- Definition of done for the active phase: the published native source and image pass preflight; one 128-chip native JobSet runs rollout DP8 x TP8 plus trainer DP8 x TP8; exactly 1,000 optimizer commits complete; full trajectory, signal, mismatch, optimizer-placement, cleanup, evaluation, and checkpoint evidence is durable; at least one finite nonzero Native serving-path mismatch is observed across A-B or B-C; every Native numerical boundary is shape-valid and finite; and the native full-stage classifier says `PASS`. The first three commits are online monitoring milestones, not an early-stop canary. This does not complete the deferred two-arm comparison.
- Task directory: `canon-zero-tim/tasks/p58-deepswe-native-zero-comparison`
- Directory state: the p58f11 original-input schema repair, regressions, and handoff were published to `yuxzhang/canon-zero-tim` as implementation commit `43614af55ed98423b757945642fa5444ae484ecc`; its first readback matched local HEAD, `FETCH_HEAD`, and the remote-tracking branch with ahead/behind `0/0`. This publication checkpoint advances the branch once more, so executors must fetch the final operator tip rather than pinning the implementation commit. No zero-TIM flag was enabled, deleted, or repurposed; `main` remains untouched.
- Current phase: P58.5N — native 128-chip full 1,000-update campaign.
- Completed/closed phases: P58.1 loss/compact-filter/accumulation contract; P58.2 paired profile, renderer, full-trajectory journal, classifier, negative controls, and exact-image regression gate. P58.3 one-host sanity was explicitly `WAIVED` by the user on 2026-08-21; it is not a PASS. P58.4N three-update was superseded by the user's direct-full decision after p58c05 failed before execution; it is not a PASS.
- Last verified fact: `p58f11` proved the p58f10 concurrency repair. The exact B8 x G16 batch ran as one 128-trajectory wave and all 8/8 prompt groups completed in 1,209.2 seconds, well inside the unchanged 3,600-second batch deadline. One row (`group_id=7`, `pair_index=14`) ended during `env.reset`; all 128 trajectories still reached learner preprocessing. Processing then raised `KeyError: 'prompts'` before the durable P58 journal, alignment, trainer forward/backward, optimizer receipt, or checkpoint.
- Local repair: `SWEEnv` now seeds `BaseTaskEnv.task` before sandbox reset with a singleton-batched prompt copied from the normalized dataset entry. The collector uses that policy-seeded environment task as the authoritative original input on both normal and pre-observation termination paths, so all 16 generations in a group share one schema. A policy-seeded task without `prompts` now fails at the collector boundary with a descriptive error instead of reaching learner merge. No trajectory is dropped, resampled, relabeled, or given policy weight; the existing timeout compact mask is unchanged.
- Validation: `git diff --check` passes. The host DeepSWE environment/renderer contract passes 8/8. Pinned image `sha256:418dc632edd8ff990e8880df6a5ca82369f6c4d705e16152c1ee6f9708d5e53a` runs the new SWEEnv and collector positive/negative controls and emits `P58_EXACT_IMAGE_CPU_PASS loss_oracle=1 weighted_accumulation=1 compact_filter=1 durable_journal=1 paired_renderer=1 alignment_policy=1 stock_observer=1 regressions=1`. A direct one-host v5p attempt was made after removing the self-created, unlocked zero-byte libtpu lock; the runtime could not obtain `CHIPS_PER_HOST_BOUNDS` from instance metadata and emitted `P58_ONEHOST_ALIGNMENT_BLOCKED reason=device_inventory_timeout timeout_secs=30`. This is not a TPU PASS.
- Next action: fetch and exactly read back the final `yuxzhang/canon-zero-tim` operator tip, then use fresh native full run-id `p58f12`. Before apply retain concurrency 128, `cpu-np`, head/worker host networking, required head anti-affinity, JobSet DNS, and exact RM/PATHWAYS_HEAD routing. Require the first durable 128-row journal (including the admitted reset-timeout compact row), finite Native boundaries/backward, a valid TPU-resident optimizer receipt, and the first commit; continue through 1,000 commits if healthy. Do not render/apply zero.
- Blockers: p58f08 through p58f11 have no optimizer receipt or checkpoint and are immutable `INCONCLUSIVE`; none has resumable training state. P58c01 through p58c05 and p58f01 through p58f11 remain attempt evidence. The published repair needs a fresh Kubernetes execution. Zero launch remains deferred. Direct one-host TPU execution is blocked by unavailable TPU metadata/device exposure and cannot validate the 128-chip Pathways path.
- Key artifacts: `plan.md`; `HANDOFF.md`; `log.md`; `phases/p58-5-native-full.md`; `../../debug_logs/p58_p58f11_deepswe_missing_prompt_key.raw.log` (SHA-256 `9bd1ca7526f38df32bde01cb4f811c464b76cc253d029b2f448cdc80164fee74`); its classification JSON (SHA-256 `1ba5c0f34171d219a4a6716c7368f86fc8968015649d577384e15ad6b4328454`); prior immutable evidence listed in `log.md`; `../../cluster/P58_DEEPSWE_TIM_RUNBOOK.md`; `../../tests/p58_deepswe_native_zero/`.
- Updated: 2026-08-22 UTC
