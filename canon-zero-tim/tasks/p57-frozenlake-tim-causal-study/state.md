# State

- Status: active
- Objective: Causally determine whether, under equal initial weights, data, topology, sampling, optimizer, and objective, finite trainer-inference mismatch worsens dense Qwen3-8B long-context FrozenLake convergence, final capability, or cross-seed stability relative to bitwise-zero mismatch, and separately quantify the systems cost of the zero-mismatch contract.
- Definition of done: a frozen, nontrivial FrozenLake recipe is selected using only stock-fast/mismatch-arm discovery outcomes and before observing any zero-arm learning outcome; the zero arm is bitwise exact; the stock arm has a finite reproducible treatment dose; paired campaigns differ only by the registered complete numerical zero-TIM bundle while all nonnumerical inputs remain equal; and the analysis reports the preregistered capability, stability, mechanism, and systems outcomes without exceeding the claim ceiling.
- Task directory: `canon-zero-tim/tasks/p57-frozenlake-tim-causal-study`
- Directory state: `p57cal4` evidence is published through `225bd6ad`; this change set carries the locally validated stock no-update sync/attestation repair
- Current phase: P57.0 — stock-fast calibration readiness (local admission complete; immutable source and hardware launch pending)
- Last verified fact: target attempt `p57cal4` connected all 64 TPU devices, materialized M10/M15/M20, loaded Qwen3-8B, initialized the KV cache, and reported approximately 34.3/95 GiB HBM per device. It then failed before `RECIPE_START` because the shared no-update boundary unconditionally called canonical adapter-backed exact weight attestation even though stock-fast deliberately skips that adapter. The local repair preserves real `update_params` synchronization, gives stock an honest transport-only receipt (`exact_weight_attestation=unavailable-by-design`), and retains fail-closed exact equality for canonical resume/evaluation. P57 host tests pass 81/81 and the complete pinned-image gate terminates with `P45_EXACT_IMAGE_CPU_PASS overlay=qwen8b_tp8`. The committed p57cal4 artifact is a 759-line workload-only log (SHA-256 `073288beee03d579533bb147dbc7af2c80e6d986eb8c74a767781263d2a04bfa`), not a complete wrapper log, so it is analysis-grade only.
- Next action: after this change set is published as an immutable SHA, render from that SHA and request a separate 64-chip `p57cal5` launch approval. Require the complete wrapper log from byte zero and exactly one stock rollout-sync marker before accepting the run.
- Blockers: no calibration recipe has started. HBM/KV startup capacity is now observed healthy, but the repaired rollout execution and complete evidence packaging remain unverified on target.
- Key artifacts: `plan.md`; `HANDOFF.md`; `RUNBOOK.md`; `phases/p57-0-readiness.md`; `phases/p57-1-stock-discovery.md`; `phases/p57-2-freeze-and-dose.md`; `phases/p57-3-paired-pilot.md`; `phases/p57-4-main-campaign.md`; `phases/p57-5-analysis.md`
- Updated: 2026-08-21 UTC
