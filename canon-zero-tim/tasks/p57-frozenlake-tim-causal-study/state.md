# State

- Status: active
- Objective: Causally determine whether, under equal initial weights, data, topology, sampling, optimizer, and objective, finite trainer-inference mismatch worsens dense Qwen3-8B long-context FrozenLake convergence, final capability, or cross-seed stability relative to bitwise-zero mismatch, and separately quantify the systems cost of the zero-mismatch contract.
- Definition of done: a frozen, nontrivial FrozenLake recipe is selected using only stock-fast/mismatch-arm discovery outcomes and before observing any zero-arm learning outcome; the zero arm is bitwise exact; the stock arm has a finite reproducible treatment dose; paired campaigns differ only by the registered complete numerical zero-TIM bundle while all nonnumerical inputs remain equal; and the analysis reports the preregistered capability, stability, mechanism, and systems outcomes without exceeding the claim ceiling.
- Task directory: `canon-zero-tim/tasks/p57-frozenlake-tim-causal-study`
- Directory state: the M15 stock machinery and direct-run contract are published; `p57_eval0_att1` through `att3` and `p57_stock_full_att1/att2` are committed analysis-grade failures; observer-only processed B was published at `de0f350f`. A local, uncommitted causal-purity repair now pins sampler IS off in both P57 arms, keeps rollout A as old logprob, forbids TIS weights, and adds a runtime/postflight receipt.
- Current phase: P57.1 — M15 stock full-curve selection (calibration complete; attempt 2 failed before backward/update 0; processed-B repair published; causal-purity repair locally green and awaiting review/publication)
- Last verified fact: the uncommitted purity repair passed the P57 host suite `102/102` with `P57_FROZENLAKE_TIM_CPU_PASS`; the pinned image matched all 34 Qwen3-8B TP8 overlay files, retained base `110/110`, P45 `40/40`, PEFT `2/2`, Agentic `4/4`, all stock runtime/observer negatives and forward/VJP probes, then ended `P57_STOCK_OBSERVER_EXACT_IMAGE_PASS targets=absolute values=processed` and `P45_EXACT_IMAGE_CPU_PASS overlay=qwen8b_tp8`. No TPU target run, commit, or push occurred.
- Next action: review the uncommitted purity diff, then request separate commit/push approval. Only after publication and separate launch approval may a fresh mismatch/stock-fast M15 `selection` attempt be rendered in checkpoint mode `new` and run continuously from 0→200.
- Blockers: publication approval, target launch approval, and target evidence are pending. All five failed attempts remain `INCONCLUSIVE` and are not resumable.
- Key artifacts: `plan.md`; `HANDOFF.md`; `RUNBOOK.md`; `phases/p57-0-readiness.md`; `phases/p57-1-stock-discovery.md`; `phases/p57-2-freeze-and-dose.md`; `phases/p57-3-paired-pilot.md`; `phases/p57-4-main-campaign.md`; `phases/p57-5-analysis.md`
- Updated: 2026-08-22 UTC
